<!DOCTYPE html>
<html lang="{{ app.request.locale }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Hello City{% endblock %}</title>
        {% block stylesheets %}
        <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
        {% endblock %}
    </head>

    <body class="flex flex-col justify-between items-center min-h-screen">
        <main role="main" class="my-6 flex flex-col items-center justify-center">
            {% block body %}{% endblock %}
        </main>
  
    <footer class="my-6">
            <p class="text-gray-500">
                &copy; Copyright {{ 'now' | date('Y') }} 
                &middot; Les Teachers du Net 
                &middot; <a class="text-indigo-500 hover:text-indigo-600 underline" href="{{ path('app_about') }}">About Us</a>
            </p>
    </footer>   
  
        {% block javascripts %}{% endblock %}
    </body>
</html>
